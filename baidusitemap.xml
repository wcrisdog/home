---
layout: null
permalink: /baidusitemap.xml
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>{{ site.url }}{{ site.baseurl }}/</loc>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>

  {% for page in site.pages %}
    {% if page.sitemap != false and page.url != '/robots.txt' and page.url != '/baidusitemap.xml' and page.url != '/sitemap.xml' and page.url != '/feed.xml' and page.url != '/404.html' %}
  <url>
    <loc>{{ site.url }}{{ site.baseurl }}{{ page.url | replace:'index.html','' }}</loc>
    {% if page.last_modified_at %}<lastmod>{{ page.last_modified_at | date_to_xmlschema }}</lastmod>{% endif %}
    <changefreq>weekly</changefreq>
    <priority>0.7</priority>
  </url>
    {% endif %}
  {% endfor %}

  {% for post in site.posts %}
  <url>
    <loc>{{ site.url }}{{ site.baseurl }}{{ post.url }}</loc>
    <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
  </url>
  {% endfor %}
</urlset>
